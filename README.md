# Fixed Frequency Signal Generator
A basic signal generator that generates the 4 fundamental waveforms (Square, Sine, Triangular and Sawtooth). The generator was built using only passive components (resistors and capacitors) and operational amplifiers. The generator has a fixed frequency at 1 kHz.
